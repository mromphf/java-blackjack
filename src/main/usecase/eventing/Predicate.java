package main.usecase.eventing;

public enum Predicate {
    ACCOUNTS_LOADED,
    ACCOUNT_CREATED,
    ACCOUNT_DELETED,
    ACCOUNT_SELECTED,
    ACTION_TAKEN,
    CURRENT_BALANCE_UPDATED,
    LAYOUT_ALERT,
    LAYOUT_CHANGED,
    TRANSACTION_ISSUED,
    TRANSACTION_SERIES_ISSUED,
    TRANSACTIONS_LOADED
}
